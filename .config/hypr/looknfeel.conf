# Change the default Omarchy look'n'feel

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    # Restore gaps between windows and borders
    gaps_in = 5
    gaps_out = 10
    border_size = 2

    # Tiger wallpaper theme border colors (high contrast)
    col.active_border = rgba(6B88A8FF) rgba(FF4830FF) 45deg
    col.inactive_border = rgba(243649AA)

    # Use master layout instead of dwindle
    # layout = master
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    # Use round window corners (matching walker)
    rounding = 16
}

# https://wiki.hypr.land/Configuring/Dwindle-Layout/
dwindle {
    # Avoid overly wide single-window layouts on wide screens
    # single_window_aspect_ratio = 1 1
}
